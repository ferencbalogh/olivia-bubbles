import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.olivia.bubbles',
  appName: 'Olivia Bubbles',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
